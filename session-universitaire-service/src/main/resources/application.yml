server:
    port:

spring:
    application:
        name: session-universitaire-service

    datasource:
        url:
        username:
        password:

    jpa:
        hibernate:
            ddl-auto: update
        generate-ddl: true
        database-platform:

    messages:
        basename: messages.messages

eureka:
    instance:
        prefer-ip-address: true
    client:
        service-url:
            defaultZone: