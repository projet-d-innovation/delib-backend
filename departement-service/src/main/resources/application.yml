server:
  port:


spring:
  application:
    name: departement-service


    datasource:
      url:
      username:
      password:

    jpa:
      hibernate:
        ddl-auto: update
      generate-ddl: true
      database-platform:

    messages:
      basename: messages.messages
      encoding: UTF-8

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone:
