server:
  port: 50018

spring:
  application:
    name: note-service

  datasource:
    url: jdbc:mysql://localhost:3306/notes-db?useSSL=true&createDatabaseIfNotExist=true
    username: root
    password: elbouchouki

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: false
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: false

  messages:
    basename: messages.messages

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
