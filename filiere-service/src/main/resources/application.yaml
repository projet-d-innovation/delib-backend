server:
  port:

spring:
  application:
    name: filiere-service

  datasource:
    url:
    username:
    password:

  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    database-platform:

  messages:
    basename: messages.messages
    encoding: UTF-8

eureka:
  client:
    service-url:
      defaultZone:
