spring:
  application:
    name: ${SPRING_SERVICE_NAME:eureka-server}

server:
  port: ${SPRING_PORT:8761}
  
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: false
    register-with-eureka: false